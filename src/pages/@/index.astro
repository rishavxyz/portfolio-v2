---
export const prerender = false;

import {Layout} from "$lib/layouts";

import { Auth } from "./utils";

const id = Astro.cookies.get("session")?.value;
const session = new Auth().getSession(id);

if (!session) return Astro.redirect("/@/auth");
---

<Layout class="p-0 space-y-1">
  Work in progress
</Layout>
